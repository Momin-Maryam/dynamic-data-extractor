# SelectorScrape
 
A configurable Chrome extension for extracting structured data from any website no coding required. Instead of hardcoding selectors for one specific site, you define your own fields (name + CSS selector), and the extension extracts matching data across the page.
